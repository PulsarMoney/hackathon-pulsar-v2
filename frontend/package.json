{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "dev:pulsarmoney": "pnpm run dev --filter pulsar.money...",
    "build:pulsarmoney": "pnpm run build --filter=pulsar.money..."
  },
  "packageManager": "pnpm@8.6.10",
  "name": "pulsarmoney",
  "dependencies": {
    "@changesets/cli": "^2.26.2",
    "eslint": "^8.48.0",
    "prettier": "^3.0.3",
    "tsconfig": "0.0.0",
    "turbo": "latest"
  }
}
